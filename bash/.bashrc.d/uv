##
# An extremely fast Python package and project manager
#
# Installation:
#     UV_NO_MODIFY_PATH=1 curl --fail --location https://astral.sh/uv/install.sh | sh
#
# @see https://docs.astral.sh/uv/getting-started/installation/
##

if [[ -n $(which uv) ]]; then
    ##
    # bash auto-completion
    # @see https://docs.astral.sh/uv/reference/cli/#uv-generate-shell-completion
    ##
    source <(uv generate-shell-completion bash)
fi
