##
# Configures the BROWSER environment variable for use within WSL
##

BROWSER='/mnt/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe'

if [[ -f "${BROWSER}" ]]; then
    export BROWSER
fi
